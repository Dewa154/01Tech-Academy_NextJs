/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'images.unsplash.com', 'images.pexels.com', 'media.istockphoto.com', 'cdn.pixabay.com', 'avatars.githubusercontent.com'],
    }
};

export default nextConfig;
